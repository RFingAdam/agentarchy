--- a/bin/oal-migrate-notify
+++ b/bin/oal-migrate-notify
@@ -41,7 +41,7 @@
 # The shell keeps the click command with the toast, so this oneshot can hand the
 # invitation over and exit instead of staying activated until it is answered.
 oal-notification-send -u critical -g  "Pending Agentarchy Migrations" "$message" \
-  --exec "oal-launch-floating-terminal-with-presentation oal-migrate" && exit 0
+  --exec "oal-run-in-terminal oal-migrate" && exit 0
 
 # Reached when the notification could not be handed off, so fall back to telling
 # the user in the terminal.
