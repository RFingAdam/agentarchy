#!/bin/bash

# oal:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/oal/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! oal-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
