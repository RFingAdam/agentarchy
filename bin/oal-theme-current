#!/bin/bash

# oal:summary=Show current theme
# oal:examples=oal theme current

THEME_NAME_PATH="$HOME/.local/state/oal/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  sed -E 's/(^|-)([a-z])/\1\u\2/g; s/-/ /g' "$THEME_NAME_PATH"
else
  echo "Unknown"
fi
