#!/bin/bash

# oal:summary=List available monospace fonts
# oal:examples=oal font list | oal font set "CaskaydiaMono Nerd Font"

fc-list :spacing=100 -f "%{family[0]}\n" | grep -v -i -E 'emoji|signwriting|oal' | sort -u
