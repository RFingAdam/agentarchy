#!/bin/bash

# oal:summary=Dismiss a notification by summary substring. Used by the first-run notifications to dismiss them after clicking for action.
# oal:args=<summary>

if (($# == 0)); then
  echo "Usage: oal-notification-dismiss <summary>"
  exit 1
fi

oal-shell -q notifications dismiss "$1"
