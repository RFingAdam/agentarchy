# AGENTS.md

Same rules as CLAUDE.md (read it). Summary for non-Claude agents:
- `oal-*` / `OAL_*` naming. `omarchy` only under `upstream/`, `docs/`, `tasks/`, `test/fixtures/`,
  `test/unit/`, `.github/`, and in `NOTICE`, `LICENSE`, `README.md`, `CLAUDE.md`, `AGENTS.md`.
- Ownership is `upstream/VENDORED-FILES.txt`: those files are regenerated by
  `bin/oal-dev-sync-upstream` and must not be hand-edited; changes go in `upstream/patches/` via
  `bin/oal-dev-upstream-patch`. Everything else is native. See `upstream/README.md`.
- `bin/oal-dev-check` must pass (eight `PASS` lines) before a PR.
